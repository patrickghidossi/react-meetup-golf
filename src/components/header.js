import React, { Component } from 'react';
import ReactDOM from 'react-dom';

class Header extends Component {
  render() {
    return(
      <h1 className="meetup-golf-header">Meetup Golf</h1>
    );
  }
}

export default Header;
